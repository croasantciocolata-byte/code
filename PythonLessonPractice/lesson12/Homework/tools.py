# In the first file (tools.py), define a
#  function called format_name(name) that returns
#  the name in uppercase letters with a greeting
#  (e.g. "Hello, ALICE!").

def format_name(name):
    return f"Hello {name.upper()}"